import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    mainDiv: {
        width: '100%',
        height: '100%',
        backgroundColor: '#fff',
        padding: 12
    }
});