import React from 'react';
import { View, Text } from 'react-native';

const MedicinesPage = ({ navigation }) => {
    return (
        <View>
            <Text> MedicinesPage </Text>
        </View>
    )
};

export default MedicinesPage;
